<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/static/style.css" />
    <link rel="stylesheet" href="/static/header.css" />
    <title>Manage Meets</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: Arial, sans-serif;
      }
      .container {
        display: grid;
        grid-template-columns: 10vw 1fr;
        height: 100vh;
      }
      .sidebar {
        background: #f0f0f0;
        min-width: 80px;
        max-width: 300px;
        border-right: 1px solid #ddd;
      }
      .main {
        padding: 2rem;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
      }
      .meet-item {
        display: flex;
        align-items: center;
        width: 100%;
        max-width: 700px;
        background: #fff;
        border: 1px solid #eee;
        border-radius: 8px;
        margin-bottom: 1rem;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.03);
      }
      .meet-img {
        width: 200px;
        height: 120px;
        background: #cce;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2rem;
        color: #888;
        border-right: 1px solid #eee;
        flex-shrink: 0;
      }
      .meet-img img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 8px 0 0 8px;
      }
      .meet-info {
        flex: 1;
        padding: 1rem;
      }
      .meet-info h3 {
        margin: 0 0 0.5rem 0;
        font-size: 1.1rem;
      }
      .meet-info p {
        margin: 0 0 0.3rem 0;
        font-size: 0.95rem;
        color: #666;
      }
      .meet-info a {
        margin-top: 0.5rem;
        color: #007bff;
        text-decoration: underline;
        font-weight: bold;
        cursor: pointer;
        display: inline-block;
      }
      @media (max-width: 700px) {
        .container {
          grid-template-columns: 1fr;
        }
        .sidebar {
          display: none;
        }
        .main {
          padding: 1rem;
        }
        .meet-item {
          flex-direction: column;
          align-items: stretch;
        }
        .meet-img {
          width: 100%;
          height: 120px;
          border-right: none;
          border-bottom: 1px solid #eee;
          border-radius: 8px 8px 0 0;
        }
      }
    </style>
  </head>
  <body>
    <header><%- include('partials/header') %></header>
    <div class="container">
      <div class="sidebar">
        <!-- Sidebar content (optional) -->
      </div>
      <div class="main">
        <h1>Manage Meets</h1>
        <% if (meets.length === 0) { %>
        <p>No meets found.</p>
        <% } else { %> <% meets.forEach(function(meet) { %>
        <div class="meet-item">
          <div class="meet-img">
            <% if (meet.image) { %>
            <img src="<%= meet.image %>" alt="Meet Image" />
            <% } else { %>
            <span>IMG</span>
            <% } %>
          </div>
          <div class="meet-info">
            <h3><%= meet.meetingName %></h3>
            <p><strong>Location:</strong> <%= meet.location %></p>
            <p><strong>Description:</strong> <%= meet.description %></p>
            <p><strong>Max:</strong> <%= meet.maxPeople %></p>
            <a href="/meet/<%= meet._id %>">View</a>
          </div>
        </div>
        <% }); %> <% } %>
      </div>
    </div>
  </body>
</html>
